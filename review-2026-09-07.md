# video-search 计划与实现 Review

日期：2026-09-07。范围：本地代码、README、handoff、已批准产品化设计、现有测试和真实测试库。本轮只列问题、影响、优先级与证据，不包含具体修改方案。

## 总体判断

项目已经具备可运行的镜头分析与搜索原型。结构化 JSON、自由文本属性、事件顺序、文本 embedding、网页搜索与播放、可读详情、Agent 搜索会话均已有实现。

已批准的“通用、可移动、多文件夹视频素材库”仍主要停留在设计阶段。CLI 可以先后把不同目录索引进同一个数据库，但这不等于已经实现目录管理、内容去重、离线素材管理和跨电脑迁移。

产品化方向与用户已确认需求基本一致。主要问题在于现有正确性缺陷、计划与实现之间的缺口，以及验收标准对检索质量和异常恢复覆盖不足。

## 检查证据与边界

- 现有自动化测试：43 项全部通过。首次运行中，本机端口监听被沙箱限制；在允许本机监听的环境中重跑后通过，环境限制不计为代码缺陷。
- 真实测试库只读检查：2 个视频、28 个 ready 镜头、28 条文本向量、0 条视觉向量。SQLite 当前 journal_mode 为 delete。
- 使用独立临时数据库及模拟分析器复现了下文明确标记的缺陷，没有重新执行真实 Mage-VL 推理。
- 历史 30 条查询 Top1 30/30 是 README/handoff 中的既有结果，本次未重跑模型检索评测，不能当作本轮新测结果。
- 浏览器交互问题来自代码检查；本轮未进行新的浏览器端到端测试。长期运行、真实移动硬盘断连和大规模性能也未实测。
- 项目源码、提示词与真实素材库没有修改；本轮仅新增本 review 文档。

优先级：P1 为现有正确性问题或首版产品交付阻塞；P2 为质量、稳定性及规模扩展事项。计划缺口与已复现缺陷分别标记。

## 一、当前实现需要修改的事项

### R01 · P1 · 已复现：保存的搜索会话可能播放另一段素材

会话保存当时的摘要、路径和 shot_id，但详情和播放始终按当前数据库中的 shot_id 查找。镜头重建会删除行；镜头主键可以复用。因此会话文本不变，并不保证它仍指向原镜头。

复现：保存素材 B 的镜头 3，删除 B 的镜头后索引素材 C，旧会话仍记录 B，但镜头 3 当前已属于 C。原先承诺的“第二个镜头保持同一结果”在重建后不可靠。

依据：[database.py:38](src/video_search/database.py#L38)、[database.py:402](src/video_search/database.py#L402)、[database.py:485](src/video_search/database.py#L485)、[server.py:96](src/video_search/server.py#L96)。

### R02 · P1 · 已复现：版本重建失败会丢掉已经可用的分析

源文件或版本变化时，现有镜头及其关联分析、文本向量等先被删除，随后才重新分段和分析。若新版本在第一个镜头失败，旧索引已不可用。

复现：包含 2 个已分析镜头的视频，切换分析版本后模拟首镜头失败，剩余镜头数变成 0。源视频没有被删除，但已经耗费推理时间生成的数据丢失。handoff 已记录这一限制，产品化迁移和版本兼容验收仍需正面覆盖它。

依据：[indexer.py:95](src/video_search/indexer.py#L95)、[database.py:402](src/video_search/database.py#L402)。

### R03 · P1 · 已复现：未完成镜头被当成正常搜索结果

镜头在分析、向量和缩略图尚未全部完成时处于 processing 状态，但搜索读取全部镜头，没有限制为 ready。

复现：将“拥抱”镜头标为 processing，查询“拥抱”仍返回它。这可能让用户看到缺少缩略图或处理失败的结果；状态统计也把全部镜头计入总数，不能表达真正可用的数量。

依据：[indexer.py:124](src/video_search/indexer.py#L124)、[database.py:460](src/video_search/database.py#L460)、[search.py:158](src/video_search/search.py#L158)。

### R04 · P1 · 已复现：语义搜索没有可靠的“无匹配”语义

只要加载查询 embedding，候选镜头即使没有对应版本向量、字面与事件也完全不命中，仍进入结果列表。一般情况下也只按相似度排名，没有区分“符合条件”和“相对接近”。

复现：给婚礼测试镜头配置一个库内不存在的 embedding 版本，搜索“火星机器人维修”，仍返回全部测试镜头且分数均为 0。对 Agent 而言，这些结果也会被包装成“搜到的素材”。

依据：[search.py:176](src/video_search/search.py#L176)、[search.py:202](src/video_search/search.py#L202)、[skills/video-footage-search/SKILL.md](skills/video-footage-search/SKILL.md)。

### R05 · P1 · 已复现：JSON 校验通过后仍可能因字段缺失而入库失败

当前校验检查容器类型和事件序号，但没有完整检查人物 role、物体 name、人数、置信度等字段的存在性、类型和合理范围。部分错误直到写数据库才出现，已经离开 Mage 输出校验重试的范围。

复现：人物只有 appearance、没有 role，ShotAnalysis 接受该输出，写入详情时出现 KeyError。模型输出看似通过了结构校验，实际上无法完整落库。

依据：[analysis.py:65](src/video_search/analysis.py#L65)、[database.py:211](src/video_search/database.py#L211)、[mage_client.py:183](src/video_search/mage_client.py#L183)。

### R06 · P1 · 已复现：单个文件消失会中止整个目录扫描

读取文件指纹发生在逐文件异常处理之前。文件在发现后被移走、磁盘断开或访问被拒绝时，异常直接退出整次扫描，后续文件不再处理。

复现：扫描发现两个文件，第一个在指纹读取时抛出 FileNotFoundError，整批退出。这与设计中“单文件失败不阻止后续文件”“断盘保留任务状态”的要求不符。

依据：[indexer.py:79](src/video_search/indexer.py#L79)、[indexer.py:93](src/video_search/indexer.py#L93)。

### R07 · P2 · 已确认：增量跳过没有覆盖全部处理配置

分段阈值和最短镜头长度改变后，segmenter.version 仍是同一个固定字符串。索引跳过判断也不检查新增 embedding、embedding 版本或缩略图是否缺失。

复现：首次索引不生成文本向量，第二次启用文本 embedder 后仍被整段跳过，向量数保持 0。已有独立 embed-text 可以手动补文本向量，但默认索引流程不会据此判断完成度；视觉向量也没有对应补齐命令。

这会使“同一版本无需再处理”与用户实际修改过的配置不一致。

依据：[media.py:79](src/video_search/media.py#L79)、[indexer.py:82](src/video_search/indexer.py#L82)、[embeddings.py:109](src/video_search/embeddings.py#L109)。

### R08 · P2 · 代码确认：网页搜索错误与并发响应处理不完整

详情读取有错误提示和请求序号保护，但普通搜索没有对应处理。连续发起两个查询时，较早请求如果较晚返回，可以覆盖新的查询结果；接口或模型异常可能让页面停留在“正在搜索”。服务端非法 limit 参数也没有转换为明确的客户端错误响应。

另外，保存会话页内重新搜索后 URL 仍指向旧会话，刷新又回到旧结果；这与稳定会话链接的使用预期容易混淆。本项未进行新的浏览器复现。

依据：[web/index.html:218](src/video_search/web/index.html#L218)、[web/index.html:235](src/video_search/web/index.html#L235)、[server.py:34](src/video_search/server.py#L34)。

## 二、已确认计划中尚未落地的产品事项

### R09 · P1 · 计划缺口：内容去重和多目录管理

数据库仍以路径字符串作为视频唯一身份，没有目录登记、目录启停、扫描范围管理或一个内容对应多个位置的能力。相同内容的不同副本会成为不同视频并分别分析、展示。

发现视频时也没有统一转换为规范绝对路径，绝对路径、相对路径或符号链接入口可能形成不同记录。因此“只显示一次”尚未达到验收条件。

依据：[database.py:26](src/video_search/database.py#L26)、[media.py:35](src/video_search/media.py#L35)；对应设计第 6–7 节。

### R10 · P1 · 计划缺口：移动硬盘素材库及离线重连

--db 支持指定数据库文件，但视频和缩略图路径直接按当前进程工作目录或旧绝对路径解析。当前没有库清单、库身份、迁移版本、备份升级、相对路径重定位、离线结果开关和重新关联入口。

现有真实库使用 delete 日志模式；设计中的 WAL、安全关闭和检查点流程也未实现。仅复制 SQLite 到其他电脑不能保证缩略图、素材定位和播放可用。当前离线素材仍可能出现在搜索中，播放时只得到 404。

依据：[database.py:15](src/video_search/database.py#L15)、[server.py:96](src/video_search/server.py#L96)；对应设计第 5、11、13.3 节。

### R11 · P1 · 计划缺口：可观察、可恢复的扫描任务

当前是同步 CLI 扫描，主要在结束时输出汇总。没有持久化任务、逐文件进度、当前阶段、停止后完成当前镜头、任务恢复入口、模型进程重启与磁盘状态反馈。

现有“重复执行相同命令，跳过成功镜头”和“无效输出改用四帧紧凑重试”是有效能力，但覆盖范围比完整任务恢复小。FFmpeg 和外部命令没有执行超时，异常卡住时也缺少受控退出边界。多个独立 index 进程还没有同库互斥保护。

依据：[indexer.py:69](src/video_search/indexer.py#L69)、[mage_client.py:163](src/video_search/mage_client.py#L163)、[analysis.py:146](src/video_search/analysis.py#L146)；对应设计第 6.5、9–10 节。

### R12 · P1 · 计划缺口：通用素材分析与可用的模型配置流程

默认提示词明确把输入称为婚礼视频，角色示例和网页推荐查询也偏婚礼。数据库允许自由值并不代表分析行为已经通用化。

普通 index/search/serve 不默认启用文本 embedding，使用者需要准确配对命令参数；也没有库级默认模型、模型缺失提示和语义能力状态。跨电脑打开已有库时，设计要求的“可以先搜索、播放，再选择补模型”尚未形成产品流程。

依据：[shot-analysis-v1.txt:1](prompts/shot-analysis-v1.txt#L1)、[cli.py:128](src/video_search/cli.py#L128)、[web/index.html:68](src/video_search/web/index.html#L68)；对应设计第 4.1、8 节。

### R13 · P1 · 计划缺口：本地启动与管理界面

目前网页只有搜索页和镜头弹窗。库选择、最近素材库、原生选目录、目录管理、任务页、系统状态页、双击启动器和受控服务关闭均未实现。

之前出现的“页面已更新、后端仍是旧进程”曾通过重启解决，但产品中还没有服务版本和所用素材库的确认信息。Skill 检查 /api/status 时，也无法确认现存服务是否使用它刚刚搜索的同一数据库；多库后可能出现会话不存在或镜头错库。

设计要求的本地 Host 检查及未来管理接口的请求保护尚未实现。当前默认仅监听本机，但仍允许任意 --host；这属于产品边界未收口，不能等同于已经完成设计中的本地控制器。

依据：[server.py:23](src/video_search/server.py#L23)、[cli.py:116](src/video_search/cli.py#L116)、[skills/video-footage-search/SKILL.md](skills/video-footage-search/SKILL.md)；对应设计第 4、9、12 节。

## 三、质量、性能与计划本身需要完善的事项

### R14 · P1（面向长素材）· 代码确认：镜头完整覆盖不等于动作完整覆盖

分段器没有最长片段限制。长时间没有明显切换的视频可能成为一个很长的片段，但超过 20 秒后仍固定最多抽 24 帧。10 分钟连续镜头的短动作可能完全不在样本内。

现有测试主要验证明显硬切和时间边界连续；没有证明淡入淡出、快速剪辑、长镜头和短动作的识别覆盖率。历史真实描述中也存在一个分段内包含多个场景的情况，但仅凭描述不能确认是漏切还是模型误述，仍需图像核验。

依据：[media.py:89](src/video_search/media.py#L89)、[analysis.py:40](src/video_search/analysis.py#L40)、[tests/test_media.py:28](tests/test_media.py#L28)。设计将“替换分段器”排除在本阶段之外，但长素材覆盖和质量边界仍需进入验收。

### R15 · P1 · 需求缺口：没有“找到所有符合镜头”的结果浏览能力

当前默认返回前 20 条，HTTP 最大 100 条，没有分页、总命中数和后续结果入口。页面显示的“找到 N 个镜头”实际是本次返回数量，不能表示全部匹配数。

即使排序准确，超过上限的相关素材也无法从当前页面继续浏览。既有验收主要关注 Top1/Top3，与用户最初希望找出所有符合镜头的目标不完全一致。

依据：[search.py:263](src/video_search/search.py#L263)、[server.py:37](src/video_search/server.py#L37)、[web/index.html:234](src/video_search/web/index.html#L234)。

### R16 · P2 · 已复现/代码确认：无模型检索与自由值筛选仍较脆弱

中文字面搜索基本把没有空格的一整句当作一个词。复现“白天户外草地”对“白天 新娘 户外草地”的字面分为 0。事件召回能补一部分动作词，但不能弥补所有人物、环境和物体查询。

属性过滤支持自由文本，但依赖显式 field:value 和子串匹配，带空格值、同义表达、否定和多条件语义均没有完整契约。“白天”和“白天，晴朗天气”等自由值的匹配体验仍需单独验收，不能仅以“不是枚举”判定已完成。

依据：[search.py:45](src/video_search/search.py#L45)、[search.py:266](src/video_search/search.py#L266)。

### R17 · P2 · 代码确认：搜索和预处理成本尚未按素材库规模验证

每次搜索加载全部镜头，再逐镜头读取人物、事件、物体、帧和向量；结构化过滤也在这些读取之后执行。没有 FTS5 或向量索引。当前 28 个镜头的可用性不能外推到上万镜头。

分析前还会执行临时视频转码、全片段计帧解码和抽帧解码；缩略图另行读取源视频。缩略图固定取中点，没有清晰度挑选。上述成本和画面质量值得实测，但本次没有给出未经测量的性能倍率。

依据：[search.py:158](src/video_search/search.py#L158)、[database.py:284](src/video_search/database.py#L284)、[mage_client.py:23](src/video_search/mage_client.py#L23)、[media.py:129](src/video_search/media.py#L129)。

### R18 · P2 · 计划与验收问题：检索质量证据不足

现有 30 条查询来自同两个婚礼视频，事件权重也用这组查询比较选出，因此它是开发回归集，不能独立证明泛化质量。

评测只要求至少命中一个预期镜头，没有衡量所有相关镜头是否找全；不支持 expected 为空，所以也没有“不应该返回结果”的验收。代码只读取前三名，却将对应倒数排名均值标为 mrr，实际计算的是 MRR@3。

“结构合法”也不代表人物、动作和事件顺序事实正确。UI 展示的 confidence 是模型给出的值，当前没有校准证据。计划需要明确语义准确率、分段质量、无结果、召回完整性、时延和资源占用的验收边界。

依据：[evaluation.py:64](src/video_search/evaluation.py#L64)、[evaluation.py:70](src/video_search/evaluation.py#L70)、[README.md](README.md)、[handoff.md:535](handoff.md#L535)。

### R19 · P2 · 计划与交付问题：文档、验证和实现状态没有统一

- README/handoff 仍保留婚礼定位和 Cloudflare 公网说明，而最新批准设计明确通用素材、本机使用、暂不做公网入口。
- README 的能力概述包含视觉向量支持，但真实库视觉向量为 0；目前是适配接口与测试能力，不是已验证的视觉检索链路。最新产品化设计明确将视觉 embedding 排除本阶段，不应将它误列为第一版交付阻塞。
- 设计写明打开已有库不需要 Mage-VL，同时启动流程又要求启动并检查 Mage-VL；二者的依赖和启动条件仍有歧义。
- 已有 API 测试不能替代目录导入、长任务停止、断盘恢复、跨路径迁移、浏览器播放和旧会话稳定性的端到端验收。本轮复现的缺陷也未被现有测试覆盖。
- 仓库仍没有提交，项目文件为 untracked；当前缺少可定位的代码发布基线。这里只记录交付状态，本轮未创建提交。

依据：[README.md](README.md)、[handoff.md](handoff.md)、[产品化设计](docs/superpowers/specs/2026-08-30-portable-video-library-productization-design.md)、[tests/test_server.py](tests/test_server.py)。

## 优先级结论

优先处理现有结果和数据正确性：R01–R06。与此同时，R09–R13 是已批准首版产品范围的主要缺口，不能用现有 CLI 原型替代验收。R14–R15 直接影响“长视频素材库”和“找到全部符合镜头”两个核心使用目标。

R07–R08、R16–R19 需要进入后续稳定性和质量清单。当前没有证据要求立刻替换 Mage-VL、升级硬件或加入视觉 embedding；这些应保持为评估事项。
