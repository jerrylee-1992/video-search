# video-search 待优化清单

更新日期：2026-09-10。

本文件集中记录当前仍未完成的产品化与质量优化项。`review-2026-09-07.md` 保留为历史审查证据；其中 R01–R08 已经修复，不再作为待办重复维护。已完成的递归扫描保护、最长镜头切分、可恢复任务、服务重试、竖屏最长边限制，以及默认 896 分辨率也不列入待办。

## P1：下一阶段产品化

### B01 · 多数据库素材库

当前 Skill、CLI、网页服务和搜索会话一次只绑定一个 SQLite 数据库。需要支持配置多个命名数据库路径，并允许用户选择某一个素材库或一次搜索全部素材库。

验收边界：

- 使用配置文件保存素材库 ID、显示名称和 SQLite 绝对路径，不使用路径分隔字符串；
- 单库和全库搜索使用同一自然语言入口；
- 跨库结果统一排序，并携带稳定的素材库 ID，不能只依赖可能冲突的 `shot_id`；
- 保存的搜索会话、详情、缩略图和视频播放都能路由回正确数据库；
- 某个数据库所在磁盘离线时跳过该库并明确提示，不影响其它在线素材库；
- Skill 可以接受用户显式指定的库名、数据库路径或“全部素材库”。

说明：当前已经可以用多次 `index` 命令把多个源目录写入同一个数据库；这与同时查询多个独立数据库是两项不同能力。

### B02 · 便携素材库重定位与离线恢复

数据库已经保存 `source_root` 和 `relative_path`，也可以直接放在移动硬盘上，但播放仍主要依赖旧绝对路径，尚未形成完整的可移动素材库协议。

验收边界：数据库、manifest 和缩略图使用稳定库身份及相对位置；源视频优先按当前卷上的相对路径解析，跨电脑盘符变化后可以重定位；离线素材默认不参与搜索，也可选择显示为不可播放结果；重新插盘或手动关联后无需重新运行 Mage 分析。

### B03 · 素材库与扫描管理界面

目前多目录导入依赖逐次执行 CLI，没有持久化的目录清单和完整管理页面。

验收边界：可以创建/打开素材库，登记多个源目录，手动扫描全部或单个目录，启用/停用目录，查看在线状态、任务进度和错误，并安全停止或恢复任务。源视频始终只读；删除分析数据与移除源目录必须是不同操作。

### B04 · 通用视频分析提示词

当前提示词仍把输入限定为“无对白婚礼视频”，与通用视频素材库定位不一致。

验收边界：新增通用提示词版本，保留人物、场景、时段、动作顺序、物体和镜头语言结构；不根据无声画面虚构音频证据；旧婚礼分析保留原版本，新提示词只触发需要升级的分析。

### B05 · 找全结果与分页

当前搜索只返回前 N 条，页面显示的是返回数量，不是全库实际命中数。

验收边界：接口返回总命中数并支持稳定分页或游标；页面可以继续加载全部匹配镜头；保存会话固定查询时的完整排序或明确的可恢复分页状态；筛选后总数与列表一致。

### B06 · 本地启动与运行状态

当前需要手动启动 OptiQ 和网页服务，网页也不能完整确认所连接的数据库与模型版本。

验收边界：提供可重复启动的本地入口，显示当前素材库、数据库路径、服务版本、Mage 状态、embedding 状态和活动任务；搜索已有索引不强制启动 Mage；管理接口保持仅本机访问，并在移动硬盘弹出前安全关闭数据库写入。

## P2：质量、规模与效率

### B07 · 跨路径内容去重

当前视频主要按绝对路径识别。同一内容位于不同文件名、目录、符号链接或移动硬盘副本时，可能重复分析和展示。

目标：用规范路径和快速文件属性做初筛，必要时流式计算完整内容哈希；同一内容只保留一份镜头分析，但记录多个可播放位置；搜索结果只显示一次。

### B08 · 跨素材检索评测

现有评测主要来自少量婚礼镜头，不能证明通用素材上的召回与事实准确性。

目标：扩充不同题材、画质、时段和镜头长度的数据；分别衡量 Top-N、全量召回、无结果、动作顺序、人物/环境/物体事实准确性和分段覆盖；开发调参集与最终验收集分离。

### B09 · M4 16GB 实机资源验证

896 已成为默认分辨率，但尚未在 M4 Mac mini 16GB 上跑完同一批 14 镜头。

目标：记录总耗时、每镜头耗时、内存压力、swap、服务重启和 Metal OOM；要求单并发 14/14 完成后再声明正式支持。资源不足时应暂停并提示用户选择 640，不静默降低正式索引质量。

### B10 · 自由值筛选与无模型检索

结构化字段允许自由文本，但当前子串匹配、中文连续文本、同义词、否定和带空格条件仍较脆弱。

目标：建立清晰的过滤语法和中文查询规范，覆盖多个条件、同义表达和无匹配场景；缺少 embedding 模型时仍提供可理解的字面与结构化搜索结果。

### B11 · 缩略图质量选择

当前默认使用镜头中间样本，没有评估模糊、闭眼、遮挡、曝光和主体可见性。

目标：只在已经抽取的样本中选择更清晰、更具代表性的帧，避免额外完整解码；保留对应源视频时间戳。

### B12 · 大规模检索性能

当前 SQLite 搜索会扫描镜头并读取关联字段与向量，小规模可用性不能直接外推到上万镜头。

目标：先建立 1 千、1 万和更大镜头规模的延迟与内存基线，再按瓶颈增加 FTS5、向量近邻索引、批量读取和缓存；优化前后保持相同搜索质量回归。

### B13 · 视觉 embedding 增益评估

当前正式素材库主要依赖 Mage 描述、文本 embedding 和结构化事件，视觉向量接口尚未证明实际收益。

目标：针对颜色、构图、服装和 Mage 遗漏物体等查询评估代表帧视觉 embedding；只有在独立测试集上带来明确增益时才进入默认索引，不把它当成动作检索的替代方案。

## 建议顺序

1. B01 多数据库素材库；
2. B02 便携重定位与 B03 管理界面；
3. B04 通用提示词与 B05 全量结果浏览；
4. B08 通用评测与 B09 M4 16GB 验证；
5. 其余 P2 项按真实规模和查询反馈推进。
